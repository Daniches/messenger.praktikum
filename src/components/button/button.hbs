<button 
    class="button button_variant_{{variant}} button_style_{{default style "rounded"}}"
    {{parameter "type" type}}
>
    {{#if icon}} 
        <span class="material-symbols-outlined">{{icon}}</span>
    {{/if}}
    
    {{#if text}}
        <span>{{text}}</span>
    {{/if}}
</button>